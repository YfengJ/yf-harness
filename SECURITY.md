# 安全政策

目前维护 0.13.x。此项目仍为 Alpha；本地优先不等于操作系统沙箱，也不等于所有数据都不离开电脑。

## 报告漏洞

请优先使用仓库 **Security → Report a vulnerability** 私密报告，附上受影响版本、最小复现和影响范围。不要把有效凭据、私人会话或可直接攻击第三方的细节放入公开 Issue。

## 信任边界

- 使用远程模型时，用户消息、选中的文件/技能/项目指令和工具结果会进入 Provider 请求；Mock 无网络。发送前确认工作区和配置。
- 自动索引跳过常见凭据文件，但不是通用 DLP。不要把秘密放入普通源码；用户主动附加的文件仍可能包含敏感内容。
- Plan/Review 限制写工具；Shell、MCP 进程与 GitHub 写操作遵守审批。批准执行的进程拥有当前用户的操作系统权限。
- Skills 是不可信指令内容，不授予权限；其脚本不会因安装自动执行。MCP 和网络返回也不授予额外权限。
- SQLite/日志保存在本机，未实施应用级数据库加密。日志脱敏是纵深防护，不保证识别任意格式的秘密。
- 本机 macOS Bundle 使用 ad-hoc 签名，未经过 Developer ID 签名和 Apple 公证。

完整边界见 [安全模型](docs/SECURITY_MODEL.md)。

## 已知未修复的可选依赖公告（2026-09-05）

可选 `llamaindex`/`frameworks` extra 引入 NLTK 3.10.3，受 [GHSA-8mgp-746c-j5xp](https://github.com/advisories/GHSA-8mgp-746c-j5xp)（PYSEC-2026-3740 / CVE-2026-81726）影响；截至审查日无已发布修复版本。公告涉及模型文件训练、加载、保存 API 的路径边界。

当前 LlamaIndex 适配器使用 `ReActAgent(tools=[])`，不暴露或调用这些 NLTK 文件操作 API；默认核心和桌面安装/Bundle 不包含 NLTK。这是基于当前调用路径的风险判断，不表示上游漏洞已修复。只需要核心和桌面时无需安装该 extra。

CI 扫描全部锁定 extras，仅对 **nltk==3.10.3 + 此公告** 接受限时例外；其他包、版本和公告仍失败。例外在 **2026-10-05** 到期（到期当天即失败），必须重新审查或升级，不能静默延期。校验脚本拒绝缺失、空白、跳过或格式错误的审计结果。

持续检查包括全历史 Gitleaks、全 extras 的 pip-audit 和每周复查。公开源码前检查当前发布树与历史；自动扫描不能证明绝不存在敏感数据。
