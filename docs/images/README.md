# 截图维护

这里预留真实 TUI 截图。请在至少 120×36 的终端中运行 `yfh tui`，分别捕获主界面、工具审批与窄屏布局；截图前使用 MockProvider 和虚构路径，清除用户名、主机名、API Key、绝对私人路径与历史会话。

建议文件名为 `tui-main.png`、`tui-approval.png`、`tui-narrow.png`。不要提交手绘或与当前版本不一致的界面图。
