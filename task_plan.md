# 任务计划：YF-Harness 第一版

## 目标
在当前空工作区中交付一个可安装、可运行、可测试、可扩展、可发布的本地优先 Python LLM Agent Harness，并以真实验证证据满足用户给出的完成定义与九类验收场景。

## 当前阶段
阶段 22：全项目审查、优化与公开发布（进行中）

### 阶段 22 验收计划
- [x] 审阅桌面/TUI、Agent/Provider、上下文、工具/审批、存储、配置、Skills/MCP/GitHub、测试与发布链路。
- [x] 为确认的缺陷建立行为回归并修复，记录未覆盖的实际限制。
- [x] 核验 UI 键盘操作、多尺寸、长输出、取消和错误反馈；完整静态、类型、测试、Eval 与安装/打包验收。
- [ ] 扫描当前文件与 Git 历史中的凭据/私人数据，补齐公开项目说明和持续安全检查。
- [ ] 推送 GitHub，确认远端 CI 后将仓库改为 PUBLIC 并核验。

变更简报：以当前 0.12.0 为基线，按复现证据改进可靠性、界面反馈与公开发布质量。保留现有模式、工具权限、会话格式与三种入口；重点验证取消/错误/并发和不可信输入。跨模块风险按 P1 管理。用户于 2026-09-05 明确授权公开，覆盖此前私密发布偏好。

## 各阶段

### 阶段 0：仓库检查与规划
- [x] 完整读取需求
- [x] 检查仓库、Git 状态和已有规划文件
- [x] 运行初始 Change Radar
- [x] 建立规划、发现、进度与验收基线
- **状态：** complete

### 阶段 1：最小核心
- [x] 创建项目脚手架和发布元数据
- [x] 实现统一领域模型、事件和异常
- [x] 实现 Provider 抽象、注册表和 MockProvider
- [x] 实现最小流式 CLI 与基础测试
- [x] 验证 `yfh run --provider mock "你好"`
- **状态：** complete

### 阶段 2：真实 Provider 与配置
- [x] 配置合并、环境变量展开和敏感字段脱敏
- [x] OpenAICompatibleProvider 流式/非流式、重试和错误归一化
- [x] 假 HTTP 服务测试与 doctor 基线
- **状态：** complete

### 阶段 3：会话与存储
- [x] SQLite schema、增量迁移和 Repository 层
- [x] 会话、消息、运行、工具、审批、用量与快照持久化
- [x] 会话恢复、搜索、归档、删除确认和导出
- **状态：** complete

### 阶段 4：工具、安全与撤销
- [x] 文件、搜索、Patch、Shell、测试和 Git 工具
- [x] workspace 路径/符号链接隔离、输出限制和环境脱敏
- [x] 审批策略、diff 预览、原子写入和最近修改撤销
- [x] 自动化安全测试
- **状态：** complete

### 阶段 5：Agent Loop
- [x] 显式状态机与统一四模式策略
- [x] 原生 Tool Calling 与 JSON 降级协议
- [x] 参数修复、重试、步骤/调用/时间/Token/成本预算
- [x] 取消、重复调用和循环检测
- **状态：** complete

### 阶段 6：TUI
- [x] Textual 响应式主界面、流式消息与多行输入
- [x] 会话、Provider、模型、模式和权限交互
- [x] 工具卡片、审批、diff、配置、诊断和帮助界面
- [x] 快捷键、命令补全、错误恢复和核心 UI 测试
- **状态：** complete

### 阶段 7：上下文工程
- [x] 项目指令优先级、手动附件和上下文组成查看
- [x] Token 预算、自动选择、结构化压缩和关键约束保留测试
- **状态：** complete

### 阶段 8：可观察性、Eval 与 Replay
- [x] 脱敏结构化日志、Trace、Token/成本统计
- [x] 20 类离线评测场景与 JSON 报告
- [x] 默认只读运行回放和诊断完善
- **状态：** complete

### 阶段 9：文档、CI 与发布
- [x] README、示例、许可证、变更日志和全部学习文档
- [x] 跨平台 CI、构建与安装验证
- [x] Ruff、格式、类型、全量测试和最终 Change Radar
- [x] 按九类场景完成最终验收
- **状态：** complete

### 阶段 10：LangChain、LlamaIndex、AutoGen 可选集成
- [x] 核对三个框架当前官方 API、版本与兼容模型配置方式
- [x] 建立统一但不掩盖能力差异的 Framework Adapter 契约
- [x] 实现 LangChain、LlamaIndex、AutoGen 真实运行入口与缺失依赖诊断
- [x] 提供无 API Key 的框架级离线 smoke 和兼容 Provider 集成测试
- [x] 接入 CLI、Doctor、可选依赖、文档、CI 与发布包
- [x] 全量验证、全新安装、私密 GitHub 推送和六组远端 CI
- **状态：** complete

### 阶段 11：原生桌面 App 与视觉重构
- [x] 建立 PySide6 / Qt Quick 桌面壳与可测试的异步 Bridge
- [x] 重构为桌面级工作区：导航、会话、聊天、运行状态、Provider/模型/模式控制
- [x] 接入现有 AgentRunner、SQLite 会话、取消与安全审批链
- [x] 加入精致的空状态、响应式布局、动效、快捷键和错误反馈
- [x] 提供 macOS `.app` 构建脚本、图标、桌面 extra 与安装文档
- [x] 完成无头 UI 测试、全量回归、实际 `.app` 构建/启动验证
- [x] 推送私密 GitHub 并等待跨平台 CI
- **状态：** complete

### 阶段 14：可信项目技能与桌面命令面板
- [x] 静态发现 `.yfh/skills`、`.agents/skills`、`.claude/skills`、Claude/Cursor 项目命令
- [x] 建立确定性命名、来源标识、大小/符号链接限制和重复名处理
- [x] 仅在用户显式选择后加载完整技能，并与权限/工具策略分离注入上下文
- [x] 接入 CLI、TUI 和桌面 `$` 命令面板，展示技能来源与说明
- [x] 增加测试、迁移文档、完整质量门、macOS App 重建和私密 GitHub 发布
- **状态：** complete

### 阶段 15：桌面界面二次重构
- [x] 审计现有 QML 布局、状态和交互契约，建立新的视觉命题
- [x] 重构导航、对话画布、输入器与检查器，减少调试面板感和无效空白
- [x] 完善响应式布局、键盘操作、焦点/悬停/禁用状态与动效
- [x] 补齐桌面契约测试、静态检查和全量回归
- [x] 重建 macOS App、用真实 Bundle 截图验收并发布私密 GitHub
- **状态：** complete

### 阶段 17：0.9 蓝图工作室桌面重构
- [x] 从真实 Bundle 截图审计布局溢出、视觉层级、长文本与功能入口
- [x] 建立全新的暖白画布、海军蓝导航与钴蓝行动色视觉系统
- [x] 重构导航、任务标题、消息流、Composer 与覆盖式检查器，消除固定行挤压
- [x] 新增命令中心、任务状态带与可发现的快捷操作，补足高频工作流
- [x] 为 1040×720、1280×800、1480×824 建立真实截图回归与溢出检查
- [x] 完成专项/全量测试、0.9.0 App 构建、私密 GitHub 推送与远端 CI
- **状态：** complete

### 阶段 18：0.10 性能、上下文与额度纵深
- [x] 对运行热路径、上下文构建/压缩、用量持久化和桌面状态做基线审计并复现缺陷
- [x] 建立可持续的压缩状态、额度聚合与性能基准契约，不伪造 Provider 余额
- [x] 实现更快的上下文估算、跨运行压缩恢复、手动压缩入口和本地额度统计
- [x] 补齐单元、集成、压力、并发/边界测试并完成两轮 review
- [x] 对齐 0.10.0 版本与文档，重建 App、发布私密 GitHub 并验证远端 CI
- **状态：** complete

### 阶段 19：DeepSeek 真实 API 纵向验收与凭据安全
- [x] 恢复干净发布状态，审计 DeepSeek 配置入口、Provider 契约和密钥边界
- [x] 仅用临时进程环境注入密钥，验证官方端点、鉴权、流式文本与真实 usage
- [x] 覆盖 CLI、Agent/工具调用、会话恢复、上下文压缩、额度聚合与桌面后端链路
- [x] 定位并修复实机缺陷，完成专项、全量、静态与安全回归
- [x] 执行变更 review、凭据泄漏扫描、私密 GitHub 推送和远端 CI 验证
- **状态：** complete

### 阶段 20：桌面对话减法与统一附件入口
- [x] 审计真实 App 截图、QML 信息架构、附件领域模型和 Controller 公共契约
- [x] 将空会话首屏压缩为一句引导，移除三条装饰性任务说明
- [x] 精简 Composer，只保留输入、统一 `+` 附件入口、必要运行控制与发送
- [x] 在左下角建立独立设置入口，将模型、模式、上下文和额度统计迁入设置/用量中心
- [x] 让 `+` 支持图片和普通文件选择、可见附件清单、移除与真实发送/上下文接入
- [x] 补齐桌面契约测试、多尺寸截图、静态检查、全量回归和最终 App 构建
- **状态：** complete

### 阶段 21：Composer 控制、工具连接、Skills 与 GitHub
- [x] 将模型、Agent/Plan、Goal 和上下文概览放回输入框底栏，并保留高级设置双向同步
- [x] 完整接通工具审批的路径、命令、参数、Diff、联网提示与四种决策
- [x] 增加 Brave Search 预设、自定义 stdio MCP、系统钥匙串凭据和真实工具发现测试
- [x] 增加 Skills 查看、新建、本地导入、GitHub 安装及安装后立即调用
- [x] 增加当前仓库 GitHub 状态、PR、Issue、Actions 与受限读写工具
- [x] 完成 0.12.0 全量门禁、App 重建、私密 GitHub 推送与远端 CI
- **状态：** complete

### 阶段 20 变更简报
- **用户可见目标：** 主对话区回归轻量聊天体验；空状态只保留一句话；额度、上下文与设置不再挤在输入框周围；左下角提供类似 Codex 的统一设置入口；附件只用一个 `+` 入口并同时支持图片与普通文件。
- **视觉命题：** “安静的工作台”——聊天画布只承载任务和回答，低频状态进入侧栏设置中心，Composer 仅展示高频动作和已选择附件，不把运行诊断做成常驻表单。
- **核心契约：** 图片仍遵守现有多模态上传开关、大小/魔数/workspace/SHA 安全边界；普通文件必须进入真实上下文而非仅显示文件名；模型、模式和额度的既有功能只能迁移，不能删除或伪造。
- **预期影响面：** `src/yfharness/desktop/qml/Main.qml`、`DesktopController` 附件/设置接口、桌面测试和使用文档；若现有 `ContentPart` 已支持文件则复用，否则以安全的文本附件上下文接入，不改变 Provider 协议。
- **必须保留：** Plan/Agent 权限边界、`/goal`、模型选择、上下文概览、额度聚合、图片显式上传许可、会话恢复、队列、审批和窄窗口可用性。
- **明确不做：** 不删除统计和设置能力，不把任意二进制文件直接发送模型，不绕过 workspace 隔离，不引入远程资源或网页壳，不借视觉简化弱化错误/审批反馈。
- **人工风险：P1。** 附件会跨 QML、Controller、ContextBuilder 和 Provider 能力边界；采用契约测试、恶意路径/大小/类型负向测试、多尺寸真实截图、无头 smoke 与全量回归控制。

### 阶段 19 变更简报
- **用户可见目标：** 使用用户提供的 DeepSeek API 授权对整个项目做真实网络验收，找出离线 Mock 无法暴露的问题并修复，同时保证密钥绝不进入仓库或 GitHub。
- **核心契约：** 密钥值只存在于单个临时进程环境；测试配置、数据库、日志、导出、截图和规划文件只允许出现环境变量名或脱敏占位符；真实请求必须验证文本、流式事件、工具调用、usage 与持久化结果。
- **预期影响面：** OpenAI-compatible Provider、CLI/Agent、上下文与 Trace/Usage、桌面 Controller；只有实机证据确认缺陷后才修改源码和相邻测试。
- **必须保留：** workspace 隔离、审批/Plan 安全策略、Provider 错误脱敏、原始消息可审计、未知成本不冒充零成本、0.10.0 现有兼容性。
- **明确不做：** 不把 API Key 写入配置或 shell 历史，不上传真实请求内容/数据库，不用高成本或破坏性提示测试，不因测试便利放宽审批与工具权限。
- **人工风险：P1。** 风险来自真实凭据、外部计费、网络差异和可能触发工具链；采用低 Token 上限、临时数据库/配置、只读优先、明确工具白名单、请求后泄漏扫描和全量回归控制。

### 阶段 18 变更简报
- **用户可见目标：** 在保持 0.9 蓝图工作室和安全边界的前提下，让长会话运行更快、更可控，并在桌面端明确看到上下文压缩和本地累计额度。
- **核心契约：** “额度”只统计本地已记录的 Token/成本与可选本地预算，不冒充供应商账户余额；压缩摘要必须跨同一会话的后续运行可恢复，并允许用户显式触发；性能优化必须有前后基准和行为等价测试。
- **预期影响面：** ContextBuilder/Compactor、SQLite Repository、CLI/桌面 Controller/QML、配置与测试；若持久化压缩状态需要 schema 变更，则只做前向迁移并覆盖旧库升级。
- **必须保留：** Provider 真实 usage 优先、估算值显式标记、每次运行 Token/成本硬限制、Plan/Goal/技能/审批/workspace 隔离和 CLI/TUI 兼容。
- **明确不做：** 不抓取或猜测 OpenAI/第三方账户余额，不用摘要删除原始消息，不把缓存跨 workspace 或 Provider 错误复用，不以牺牲安全检查换取速度。
- **人工风险：P1。** 预计触及上下文和持久化公共契约；采用旧库迁移、确定性压缩、额度聚合边界、热路径基准、桌面多尺寸、全量回归和最终 Bundle 共同验证。

### 阶段 17 变更简报
- **用户可见目标：** 完全替换当前低对比黑色编辑台，解决文本框/按钮挤出边界、信息层级弱与功能入口不足，交付一套能长期使用的桌面 Agent 工作区。
- **视觉命题：** “蓝图工作室”——暖白纸面承载任务与对话，深海军蓝只用于导航，钴蓝作为唯一主要行动色，薄荷绿表达安全/完成状态；通过排版与结构建立产品感，不靠堆叠卡片。
- **信息架构：** 左侧为可折叠任务轨道；顶部为项目/目标/运行状态；中央为连续消息文档；底部 Composer 分为输入、任务状态带和自适应动作区；上下文/变更/配置使用覆盖式检查器，不压缩主画布。
- **新增能力：** `⌘K` 命令中心统一提供新任务、Plan、审查变更、上下文、技能与检查器入口；任务状态带持续显示 Goal、上下文、队列和变更，而不是把所有信息塞进按钮文本。
- **响应式契约：** 1040px 起可用；所有用户文本必须 elide/wrap；动作区使用可换行或按优先级收纳策略；任何窗口尺寸都不得出现水平滚动、控件越界或焦点被底栏遮挡。
- **必须保留：** 会话、Provider/模型、Agent/Plan、Goal、技能、附件、队列、审批、上下文来源、变更审查/撤销、工作区隔离和快捷键安全契约。
- **明确不做：** 不更换 Python/Qt 技术栈，不引入远程字体或网页壳，不借 UI 重构放宽审批/工具权限，不用固定截图数据替代真实状态。
- **人工风险：P2。** 主要风险是 QML 布局/焦点/弹层状态与旧功能入口回归；采用多尺寸 Bundle 截图、对象级 UI 测试、qmllint、无头 smoke 与全量回归验证。

### 阶段 16：Composer 工作流控制与持久目标
- [x] 审计现有 Plan、工作流、模型、上下文快照与会话持久化契约
- [x] 在输入框工具条加入 Plan/Agent 快速切换、模型选择器和上下文概览入口
- [x] 实现 `/goal` 会话级目标：创建、查看、更新、完成与清除，并在后续运行中稳定注入
- [x] 让上下文概览展示预算、已用比例、来源数量和可展开来源，不伪造精确 Token
- [x] 保持右侧控制台作为高级配置入口，并让 Composer 与控制台状态双向一致
- [x] 补齐数据库迁移、Controller/QML、CLI/桌面专项测试、文档与版本元数据
- [x] 执行静态检查、全量回归、Eval、macOS App 重建、真实截图和私密 GitHub CI
- **状态：** complete

### 阶段 16 变更简报
- **用户可见目标：** 像 Codex 一样，把最常用的 Plan、`/goal`、模型和上下文信息放到输入框附近，用户不打开控制台也能理解并控制下一次运行。
- **交互命题：** Composer 底栏是“本次运行控制面”：左侧为附件，中央依次为 Agent/Plan、目标、模型与上下文，右侧为发送；每个入口用轻量弹层展开，不把输入框重新做成拥挤表单。
- **Goal 契约：** `/goal <目标>` 创建或替换当前会话的持久目标；`/goal` 查看；`/goal done` 完成；`/goal clear` 清除。活动目标在每次运行中作为高优先级、可见的会话上下文注入，但不扩大权限或自动启动后台任务。
- **Plan 契约：** Composer 的 Plan 选择直接使用现有 `AgentMode.PLAN` 与只读工具边界；已生成计划继续通过现有“执行此计划”路径切换到 Agent，不复制核心状态机。
- **模型契约：** 选择项来自当前配置的 Provider/Model，模型和推理强度分离；本阶段先暴露已配置模型，不在线枚举或静默切换 Provider。
- **上下文契约：** 显示最后一次真实 ContextSnapshot 的 estimated/budget tokens、占用比例、来源数量与压缩标记；运行前明确显示“待刷新”，不把旧快照冒充当前值。
- **预期影响面：** SQLite schema/repository、DesktopController、Main.qml、桌面/存储测试、桌面文档、版本与发布证据。
- **必须保留：** workspace 隔离、Plan 只读边界、审批拒绝优先、技能渐进披露、会话/队列/执行计划、CLI/TUI 兼容和现有右侧高级控制台。
- **明确不做：** 不实现 Codex 账户/云端 Goal，不自动循环执行长期目标，不新增远程模型发现，不暴露隐藏推理内容，不让 Goal 绕过 mode、Profile、Hook 或审批策略。
- **人工风险：P1。** 新增前向数据库迁移并改变桌面发送路径；必须验证旧数据库升级、目标隔离/恢复、Plan 不写入、模型状态一致和上下文旧值重置。

### 阶段 15 变更简报
- **用户可见目标：** 把当前“左会话 + 空对话区 + 常驻调试表单”的界面彻底重做成紧凑、成熟、可长期使用的桌面 Agent 工作台。
- **视觉命题：** 精密编辑台——石墨黑主画布、温暖纸白文字、低饱和铜金作为单一强调色；重点不是堆卡片，而是用编辑排版、细线分区与明确工具条建立专业感。
- **预期影响面：** `Main.qml` 的窗口骨架、会话导航、消息流、Composer、检查器、空/忙/错状态及桌面截图；仅在必要时调整 Controller 暴露属性和桌面测试。
- **必须保留：** 现有发送/取消、审批、队列、会话切换/分支、工作区选择、Profile/Provider/模型/模式/权限、上下文/变更审查与技能命令面板功能。
- **明确不做：** 不改 Agent 核心协议、Provider、SQLite schema 或权限边界；不引入网页壳、外部字体下载或运行时网络资源。
- **人工风险：P2。** 主要风险是 QML 状态绑定、窄窗口响应式和键盘交互回归，采用桌面专项测试、无头 smoke、真实 Bundle 截图和全量测试验证。

### 阶段 14 变更简报
- **用户可见目标：** 在独立桌面 App 内直接发现并运行项目里的 Codex、Claude Code 和 Cursor 工作流，通过 `$技能名` 命令面板完成复用，不需要回到终端记命令。
- **预期影响面：** 新技能目录领域层、上下文构建与 AgentRunner、CLI/TUI、桌面 Controller/QML、Trace、测试、文档和打包版本。
- **必须保留：** 技能不能扩大 mode、Policy、WorkspaceGuard、Profile/Hook 或审批权限；不执行技能附带脚本；不扫描用户主目录；完整正文只在显式调用时进入模型上下文；Trace 只记录来源元数据而不持久化技能正文。
- **需验证假设：** 三类 frontmatter 和参数占位符兼容；同名技能消歧稳定；Windows 换行与三平台路径一致；桌面输入法、键盘选择和鼠标选择都不破坏普通消息输入。
- **明确不做：** 隐式模型触发、用户级全局技能、任意脚本或网络命令执行、插件市场安装、技能声明直接授予工具权限。
- **人工风险：P1。** 上下文与公共交互契约会变化，必须覆盖恶意技能内容、符号链接、重复名、正文不持久化、CLI/TUI/桌面集成和最终 Bundle 验证。

### 阶段 12：现代桌面 Agent 工作台全方位升级
- [x] 建立 Codex、Claude Code、Cursor 官方能力矩阵并明确安全取舍
- [x] 实现统一项目规则/记忆发现与可解释上下文视图
- [x] 实现运行中消息队列、计划到执行工作流与状态持久化
- [x] 实现 Git/文件变更审查、检查点与安全撤销工作流
- [x] 重构桌面检查器、活动时间线、快捷操作和空/错/忙状态
- [x] 补齐测试、文档、桌面打包、跨平台 CI 与私密仓库发布
- **状态：** complete

### 阶段 13：可扩展工作流与性能纵深
- [x] 设计版本化工作流 Profile、工具 allowlist 与 Pre/Post hooks 契约
- [x] 将 Profile/Hook 接入 AgentRunner、CLI、Trace、TUI 和桌面工作台
- [x] 增加多模态附件入口，保持本地预览与明确上传边界
- [x] 设计 MCP/插件能力发现与权限协商，不绕过现有审批链
- [x] 为可选隔离研究/并行任务定义上下文、工作区和合并边界
- [x] 对 491 MB macOS Bundle 与项目索引做体积、启动和增量性能剖析
- **状态：** complete

### 阶段 13 变更简报
- **用户可见目标：** 用少量可解释的工作流 Profile 组合模式、权限、工具暴露和 Hook，让不同任务可以安全复用，而不是每次手工拼装所有开关。
- **预期影响面：** 配置 schema/优先级、AgentRunner 工具定义、ToolExecutor 执行门、Agent 事件/Trace、CLI、桌面 Controller/QML、测试和文档。
- **必须保留：** mode/Policy/WorkspaceGuard 的拒绝永远不能被 Hook allow 覆盖；未选择 Profile 时保持 0.4 行为；旧配置继续可读；Post Hook 不得伪装成执行前阻止。
- **需验证假设：** glob 在三平台一致；Profile allow/deny 同时过滤模型可见定义和执行；配置深合并不会意外继承被删除的数组；Hook 事件序列可持久化且不泄露参数中的密钥。
- **明确不做：** 第一批不运行任意 shell/HTTP Hook、不自动启用第三方插件、不把 MCP annotations 当可信风险声明、不以新增模式数量代替清晰工作流。
- **人工风险：P1。** 新差异会跨配置、工具执行和 UI 公共契约，验证包含负向权限测试、CLI/桌面集成、全量矩阵与构建。

## 变更简报
- **用户可见目标：** `yfh` 在无 API Key 时以 MockProvider 完整运行，有配置时连接 OpenAI 兼容服务，并通过 TUI/CLI 安全执行 Agent 工作流。
- **预期影响面：** Python 包、CLI/TUI 公共交互、TOML 配置、环境变量、SQLite schema、工具与审批策略、日志与导出格式、测试/CI、文档。
- **必须保留：** 当前目录中的任何用户文件；当前检查表明目录为空且不是 Git 仓库，后续如出现外部修改必须避让。
- **需验证假设：** Python 3.12+ 与依赖可用性；Windows 的进程取消和原子替换差异；Textual 测试终端行为；兼容服务的 SSE 变体。
- **明确不做：** 训练/微调、分布式多 Agent、云端账户、浏览器自动化、向量数据库、大型 RAG、远程执行平台。

### 阶段 10 变更简报
- **用户可见目标：** 安装相应 extra 后，用户能通过 `yfh frameworks` 发现并真实运行 LangChain、LlamaIndex、AutoGen，既支持离线验证，也能连接现有 OpenAI 兼容 Provider。
- **预期影响面：** 可选依赖、框架适配子包、CLI/Doctor、配置到框架客户端的映射、测试、文档、CI 和发布元数据。
- **必须保留：** 默认安装不引入三套大型框架；现有 AgentRunner、安全工具、Mock、CLI/TUI 和配置契约保持兼容；API Key 仍不进入日志或持久化。
- **需验证假设：** 三套框架 2026 当前 API；异步调用和用量返回差异；非 OpenAI base_url 参数；Python 3.12/3.13 与 Windows 支持。
- **明确不做：** 用框架替换 YF-Harness 核心状态机，或把第三方框架的任意工具执行绕过现有安全边界。

### 阶段 11 变更简报
- **用户可见目标：** 不再把 TUI 当主界面，交付可双击启动的独立桌面窗口和 macOS `.app`，并彻底提升视觉、交互与信息层级。
- **视觉命题：** 深墨色工作台配一条暖琥珀强调色，像一款安静、精密的本地开发工具；主要空间留给对话而非卡片装饰。
- **内容计划：** 左侧会话导航；中央对话与输入；顶部任务上下文和运行状态；右侧按需展开的模型/模式设置；首次启动使用明确空状态。
- **交互命题：** 首屏元素分层淡入、运行状态呼吸提示、侧栏/检查器平滑展开；所有动效快速克制且不阻塞操作。
- **预期影响面：** 新桌面子包与 QML 资源、异步线程桥、依赖 extras、打包配置/脚本、图标、CLI 入口、测试、文档和 CI。
- **必须保留：** 原有 CLI/TUI 可继续运行；核心 AgentRunner、Provider、SQLite、安全审批与密钥规则不复制、不绕过；用户数据仍在 platformdirs 目录。
- **明确不做：** 用网页伪装桌面程序、要求用户先打开终端、把未经签名的构建声称为可公开分发的正式安装包，或为视觉重构改写核心执行协议。

### 阶段 12 变更简报
- **用户可见目标：** 把 YF-Harness 从“能聊天和调用工具的桌面壳”升级为能理解项目规则、先规划再执行、运行中接受后续任务、清楚展示并安全撤销每次改动的本地 Agent 工作台。
- **预期影响面：** 上下文发现与排序、Agent 运行编排、SQLite Repository、文件变更记录、桌面 Controller/QML、CLI 兼容、测试、文档、打包和 CI。
- **必须保留：** 所有现有会话和 schema 可向前迁移；默认权限不扩大；workspace 路径隔离、审批拒绝、密钥脱敏和 CLI/TUI 兼容继续成立。
- **需验证假设：** 项目规则的跨工具兼容优先级；队列在取消/失败后的行为；撤销对外部并发编辑的冲突处理；非 Git 工作区的降级体验。
- **明确不做：** 无隔离的后台云执行、静默上传代码、默认跳过审批、自动覆盖用户并发改动，或伪装成完整 IDE 编辑器。

## 风险与契约
- **总体人工风险：P1。** 初始扫描因目录为空显示 P3，但完整实现将触及安全边界、子进程、持久化 schema、公共 CLI/配置和跨平台行为。
- **公共契约：** `yfh` 命令与退出码、配置键/优先级、Provider 事件流、Tool Schema/ToolResult、SQLite 迁移、TUI 命令与快捷键、导出/回放 JSON。
- **安全契约：** workspace 不可逃逸、模型输出不直接执行、审批拒绝绝不执行、密钥不持久化/不记录、full_auto 默认关闭。
- **验证策略：** 每阶段先运行邻近测试，再运行 Ruff/类型检查；最终执行全量测试、构建/安装、Mock E2E、CLI/TUI smoke 和安全回归。

## 已做决策
| 决策 | 理由 |
|------|------|
| 采用 `src/` 布局和 `pyproject.toml` | 避免源码目录误导入，兼容 uv/pip/venv 与标准构建 |
| 核心只依赖底层库，不采用高级 Agent 框架 | 保持 Harness 机制可学习、可测试、供应商无关 |
| 统一事件流是 Provider 与 Agent/UI/日志的唯一边界 | 防止供应商原始字典污染其余系统 |
| SQLite 只通过 Repository 访问 | 隔离 SQL 和迁移契约，便于测试与未来替换 |
| 四种模式共享一个 AgentRunner | 通过策略差异复用状态机，避免行为分叉 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 初始 `/goal` 已被“读取附件”占用 | 1 | 完成该已满足目标后创建完整工程目标 |
| `git status` 报当前目录不是 Git 仓库 | 1 | 记录为初始事实；不擅自初始化或依赖 Git diff |
| Ruff `allowed-confusables` 配置类型错误 | 1 | 从 TOML 子表改为 lint 配置中的字符列表 |
| 阶段 2 CLI 补丁上下文不匹配 | 2 | 确认未部分写入，读取格式化后内容并拆分补丁 |
| 质量检查编排脚本语法错误 | 1 | 拆分工具调用，项目命令随后正常执行 |
| 环境拒绝 `rm -rf` 清理测试目录 | 1 | 使用精确文件删除和 `rmdir` 安全清理 |
| 系统 pip 因本机 CA 链无法下载依赖 | 1 | 使用 uv 在全新 venv 正常解析并安装 wheel，完整 smoke 通过 |
| uv 离线新环境缺少缓存的 Pydantic wheel | 1 | 改用 uv 正常解析；未修改依赖约束或绕过运行时依赖 |
| PyPI 版本查询的 jq 命令引号损坏 | 1 | 改用不插入 shell 变量的固定 jq TSV 过滤器，成功取得六个官方包版本 |
| LlamaIndex `FunctionAgent` 拒绝普通 MockLLM | 1 | 依据运行时约束改用官方 Workflow `ReActAgent`，并实现 `CustomLLM` 离线响应 |
| AutoGen 无工具 Agent 仍校验 function calling 能力 | 1 | 按 `ChatCompletionClient.model_info` 契约声明能力，离线和远程 smoke 均通过 |
| LlamaIndex 远程用量初次落入估算 | 1 | 从 `AgentOutput.raw.usage` 归一化真实 prompt/completion token，并加断言 |
| Nuitka 4.1 不支持 `--macos-app-identifier` | 1 | 移除无效参数，在签名前用 PlistBuddy 设置 Bundle 名称与反向域名标识 |
| 仅安装 PySide6 Essentials 时部署器解析到缺失的 StateMachine QML framework | 1 | 桌面依赖改回官方完整 PySide6 组合，避免生成缺失运行库的脆弱 Bundle |
| 首轮远端桌面 Job 缺少 Linux `libEGL.so.1`，核心矩阵未安装 Qt 时 mypy 分析 GUI 失败 | 1 | 桌面 Job 安装 libegl1 并独立 mypy；核心 mypy 排除可选桌面目录，职责分层验证 |
| Windows 3.13 TUI 集成测试偶发在会话 Worker 完成前断言 | 1 | 测试等待 Textual Worker 明确完成；复跑后全部八个 CI Job 通过 |
| 阶段 12 首批规则解析代码有两个未使用 import 和一个超长签名 | 1 | 按 Ruff 精确提示移除 import 并拆分签名，随后重跑静态与专项测试 |
| FileChangeRecord 首次映射漏掉数据库已有的 `tool_call_id` | 1 | 补齐可空字段，保持 schema 不变并重跑审查/撤销专项测试 |
| 新检查器两个 ListView footer 直接引用裸 `count` 导致 QML ReferenceError | 1 | 为规则/变更列表增加显式 ID，并通过 ID 读取 count 后重跑无头启动 |
| 桌面测试退出时已创建但未执行的刷新 coroutine 产生 RuntimeWarning | 1 | 后台队列改为提交 coroutine factory，任务真正开始时才创建异步对象 |
| 本地项目索引首轮检查有一处 import 排序和两条超长测试行 | 1 | 拆分测试数据并用 Ruff 机械整理 import，随后执行索引专项验证 |
| 桌面上下文 Token 从宽泛 payload 直接 `int()` 未通过严格 mypy | 1 | 增加拒绝 bool/非法字符串的 `_safe_int` 收窄函数 |
| 0.4 全量发布门有四个文件不符合 Ruff formatter 输出 | 1 | 对精确文件运行项目格式器后重新执行全部静态、类型与覆盖率门 |
| 桌面 mypy 首次把目录作为参数时因项目 exclude 规则未发现文件 | 1 | 改为显式列出三个桌面 Python 源文件，保持可选 GUI 与核心门禁分层 |
| 0.4 首轮 Windows CI 的 Cursor frontmatter 与审查快照测试受 CRLF 文本转换影响 | 1 | 解析器先归一化 frontmatter 换行；字节级冲突测试改用 `write_bytes`，不放宽生产哈希比较 |
| 0.5 工具执行接线首轮 Ruff 报一个类型名 import 次序 | 1 | 使用项目 Ruff 精确整理该 import block 后重跑静态与专项测试 |
| 0.5 CLI 组合补丁因预期的重复 `context` 行与实际文件不一致而原子拒绝 | 1 | 确认无部分写入，按 import/命令签名/运行接线/输出分成小补丁 |
| `uv sync --extra dev --extra desktop` 移除了框架可选依赖，导致全目录 mypy 报缺包 | 1 | 使用锁文件恢复 `dev + desktop + frameworks` extras，随后 67 个源码文件类型检查通过 |
| Nuitka 首次构建询问是否下载 ccache | 1 | 拒绝下载，并在部署参数加入 `--disable-cache=ccache`，保证后续构建非交互 |
| Bundle 体积审计把递归 QML 数据目录误判为顶层 Qt 动态库 | 1 | 将禁用模块审计限制到 `Contents/MacOS` 顶层；实际重型二进制未进入产物 |
| macOS 环境没有 GNU `timeout` | 1 | 用 PTY 启动无头 Qt，确认 QML 加载后发送 Ctrl+C 回收 |
| 阶段 15 首次 QML smoke 报对话列表 anchor loop | 1 | 居中边距从依赖 ListView 自身宽度改为依赖父容器宽度，消除循环绑定 |
| 阶段 15 测试与文档组合补丁未匹配旧快捷键原文 | 1 | 确认补丁原子拒绝后读取精确行，按现有文案重新应用 |
| `pyside6-qmllint` 报两处 Layout 子项直接设置宽高 | 1 | 改用 `Layout.preferredWidth/Height`，其余 contextProperty unqualified 提示为现有动态 Controller 模式 |
| 阶段 16 首轮 Ruff 报 Goal 状态文案全角括号，格式器要求合并一处字符串 | 1 | 改用中点分隔状态并运行项目格式器，行为测试 24 项已通过 |
| 阶段 16 两个 Composer Popup 被 qmllint 视为 Layout 管理子项 | 1 | 运行 smoke 正常；改为显式 Overlay parent 并复跑布局警告过滤 |
| 阶段 16 模型切换用例插入既有两消息断言之前导致 4 != 2 | 1 | 模型切换本身成功；拆为独立桌面测试，保留原测试的精确消息契约 |
| 阶段 16 桌面 mypy 收尾命令引用不存在的 `models.py` | 1 | 读取真实模块清单后对整个 `src/yfharness/desktop` 运行，3 个源文件全部通过 |
| 0.8.0 首个 Bundle 缺少 `CFBundleVersion` | 1 | 构建脚本从包版本写入短版本与构建版本并强制校验；重建后两者均为 0.8.0 |
| 阶段 17 首个三文件规划补丁因 findings 开头上下文未匹配而拒绝 | 1 | 确认无部分写入，拆成按文件精确锚点的小补丁 |
| 阶段 17 响应式测试首轮 Ruff 报 import 顺序与 106 字符断言 | 1 | 调整标准库 import 并把预期控件名拆为集合变量 |
| 布局报告使用 `dict[str, object]` 后直接索引嵌套字典未通过 mypy | 1 | 用独立强类型 `items` 字典组装报告，消除 object 索引赋值 |
| 三尺寸报告的 `findChild` 未穿过 QML 视觉树，控件集合为空 | 1 | 改为从窗口 contentItem 显式遍历 `QQuickItem.childItems()` |
| 1040px 压力截图中超长会话标题覆盖状态与命令按钮 | 1 | 为标题 Column 内的路径行和标题绑定父宽度，使 `ElideRight` 真正生效 |
| 自定义 QQuickItem 遍历替代 `findChild` 后遗留未使用 QObject import | 1 | 删除不再使用的类型 import 并重跑桌面门禁 |
| 第二轮视觉/键盘收尾组合补丁误把 findings 标题用作 QML 上下文 | 1 | 确认原子拒绝无部分写入，按代码与规划记录拆分 |
| 命令搜索过滤后 `currentIndex` 可能停在隐藏行 | 1 | 查询时选中首个匹配项，上下键循环跳过不匹配行 |
| 检查器抽屉打开时标题栏旧关闭按钮像控件越界 | 1 | 抽屉内保留关闭动作，标题栏入口在打开期间隐藏 |
| 抽屉右侧 16px 边距露出底层“命令”按钮尾部 | 1 | 改用 Overlay 宽度定位并贴合右边界、覆盖完整窗口高度 |
| Bundle 验收尾部使用系统 `python` 美化 JSON，但精简打包环境没有该命令 | 1 | 前序签名与四张截图均已成功；改用已有 `jq` 验证报告，13 项全部在窗口内 |
| 会话列表异步刷新把缺失的 instructions 当成空列表，清掉运行上下文来源 | 1 | 仅在 payload 明确包含 instructions 时更新基础来源，并保留 Goal 来源直接断言 |

## 备注
- `PLAN.md` 是面向项目用户的阶段与验收计划；本文件是技能要求的持续工作记忆。
- 第一版九阶段全部完成；后续变更以 `docs/ROADMAP.md` 和新任务为入口。
