# 任务计划：YF-Harness 第一版

## 目标
在当前空工作区中交付一个可安装、可运行、可测试、可扩展、可发布的本地优先 Python LLM Agent Harness，并以真实验证证据满足用户给出的完成定义与九类验收场景。

## 当前阶段
第一版已完成；进入维护状态

## 各阶段

### 阶段 0：仓库检查与规划
- [x] 完整读取需求
- [x] 检查仓库、Git 状态和已有规划文件
- [x] 运行初始 Change Radar
- [x] 建立规划、发现、进度与验收基线
- **状态：** complete

### 阶段 1：最小核心
- [x] 创建项目脚手架和发布元数据
- [x] 实现统一领域模型、事件和异常
- [x] 实现 Provider 抽象、注册表和 MockProvider
- [x] 实现最小流式 CLI 与基础测试
- [x] 验证 `yfh run --provider mock "你好"`
- **状态：** complete

### 阶段 2：真实 Provider 与配置
- [x] 配置合并、环境变量展开和敏感字段脱敏
- [x] OpenAICompatibleProvider 流式/非流式、重试和错误归一化
- [x] 假 HTTP 服务测试与 doctor 基线
- **状态：** complete

### 阶段 3：会话与存储
- [x] SQLite schema、增量迁移和 Repository 层
- [x] 会话、消息、运行、工具、审批、用量与快照持久化
- [x] 会话恢复、搜索、归档、删除确认和导出
- **状态：** complete

### 阶段 4：工具、安全与撤销
- [x] 文件、搜索、Patch、Shell、测试和 Git 工具
- [x] workspace 路径/符号链接隔离、输出限制和环境脱敏
- [x] 审批策略、diff 预览、原子写入和最近修改撤销
- [x] 自动化安全测试
- **状态：** complete

### 阶段 5：Agent Loop
- [x] 显式状态机与统一四模式策略
- [x] 原生 Tool Calling 与 JSON 降级协议
- [x] 参数修复、重试、步骤/调用/时间/Token/成本预算
- [x] 取消、重复调用和循环检测
- **状态：** complete

### 阶段 6：TUI
- [x] Textual 响应式主界面、流式消息与多行输入
- [x] 会话、Provider、模型、模式和权限交互
- [x] 工具卡片、审批、diff、配置、诊断和帮助界面
- [x] 快捷键、命令补全、错误恢复和核心 UI 测试
- **状态：** complete

### 阶段 7：上下文工程
- [x] 项目指令优先级、手动附件和上下文组成查看
- [x] Token 预算、自动选择、结构化压缩和关键约束保留测试
- **状态：** complete

### 阶段 8：可观察性、Eval 与 Replay
- [x] 脱敏结构化日志、Trace、Token/成本统计
- [x] 20 类离线评测场景与 JSON 报告
- [x] 默认只读运行回放和诊断完善
- **状态：** complete

### 阶段 9：文档、CI 与发布
- [x] README、示例、许可证、变更日志和全部学习文档
- [x] 跨平台 CI、构建与安装验证
- [x] Ruff、格式、类型、全量测试和最终 Change Radar
- [x] 按九类场景完成最终验收
- **状态：** complete

## 变更简报
- **用户可见目标：** `yfh` 在无 API Key 时以 MockProvider 完整运行，有配置时连接 OpenAI 兼容服务，并通过 TUI/CLI 安全执行 Agent 工作流。
- **预期影响面：** Python 包、CLI/TUI 公共交互、TOML 配置、环境变量、SQLite schema、工具与审批策略、日志与导出格式、测试/CI、文档。
- **必须保留：** 当前目录中的任何用户文件；当前检查表明目录为空且不是 Git 仓库，后续如出现外部修改必须避让。
- **需验证假设：** Python 3.12+ 与依赖可用性；Windows 的进程取消和原子替换差异；Textual 测试终端行为；兼容服务的 SSE 变体。
- **明确不做：** 训练/微调、分布式多 Agent、云端账户、浏览器自动化、向量数据库、大型 RAG、远程执行平台。

## 风险与契约
- **总体人工风险：P1。** 初始扫描因目录为空显示 P3，但完整实现将触及安全边界、子进程、持久化 schema、公共 CLI/配置和跨平台行为。
- **公共契约：** `yfh` 命令与退出码、配置键/优先级、Provider 事件流、Tool Schema/ToolResult、SQLite 迁移、TUI 命令与快捷键、导出/回放 JSON。
- **安全契约：** workspace 不可逃逸、模型输出不直接执行、审批拒绝绝不执行、密钥不持久化/不记录、full_auto 默认关闭。
- **验证策略：** 每阶段先运行邻近测试，再运行 Ruff/类型检查；最终执行全量测试、构建/安装、Mock E2E、CLI/TUI smoke 和安全回归。

## 已做决策
| 决策 | 理由 |
|------|------|
| 采用 `src/` 布局和 `pyproject.toml` | 避免源码目录误导入，兼容 uv/pip/venv 与标准构建 |
| 核心只依赖底层库，不采用高级 Agent 框架 | 保持 Harness 机制可学习、可测试、供应商无关 |
| 统一事件流是 Provider 与 Agent/UI/日志的唯一边界 | 防止供应商原始字典污染其余系统 |
| SQLite 只通过 Repository 访问 | 隔离 SQL 和迁移契约，便于测试与未来替换 |
| 四种模式共享一个 AgentRunner | 通过策略差异复用状态机，避免行为分叉 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 初始 `/goal` 已被“读取附件”占用 | 1 | 完成该已满足目标后创建完整工程目标 |
| `git status` 报当前目录不是 Git 仓库 | 1 | 记录为初始事实；不擅自初始化或依赖 Git diff |
| Ruff `allowed-confusables` 配置类型错误 | 1 | 从 TOML 子表改为 lint 配置中的字符列表 |
| 阶段 2 CLI 补丁上下文不匹配 | 2 | 确认未部分写入，读取格式化后内容并拆分补丁 |
| 质量检查编排脚本语法错误 | 1 | 拆分工具调用，项目命令随后正常执行 |
| 环境拒绝 `rm -rf` 清理测试目录 | 1 | 使用精确文件删除和 `rmdir` 安全清理 |
| 系统 pip 因本机 CA 链无法下载依赖 | 1 | 使用 uv 在全新 venv 正常解析并安装 wheel，完整 smoke 通过 |
| uv 离线新环境缺少缓存的 Pydantic wheel | 1 | 改用 uv 正常解析；未修改依赖约束或绕过运行时依赖 |

## 备注
- `PLAN.md` 是面向项目用户的阶段与验收计划；本文件是技能要求的持续工作记忆。
- 第一版九阶段全部完成；后续变更以 `docs/ROADMAP.md` 和新任务为入口。
